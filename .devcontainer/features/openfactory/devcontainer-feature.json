{
  "id": "openfactory",
  "version": "0.1.0",
  "name": "OpenFactory SDK Feature",
  "description": "Installs the OpenFactory SDK and its local infrastructure.",
  "options": {
    "installInfra": {
      "type": "boolean",
      "default": true,
      "description": "Copy Docker Compose and infra files"
    }
  }
}
